## Exercise 2: commit-one-file

Instructions: two files (A.txt, B.txt) created in root, both staged. Goal is to commit only one.

Ran:
```bash
git start commit-one-file
```

`ls` showed A.txt, B.txt, README.md, start.sh.

Ran:
```bash
git add A.txt
git commit -m "add one file"
git verify
```
Passed.